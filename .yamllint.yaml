# Lint-only rules that yamlfmt does not cover.
extends: default
rules:
  anchors: enable
  comments:
    min-spaces-from-content: 1
  document-end: disable
  document-start: disable
  line-length: disable
  truthy:
    ignore: .github/workflows/*.y*ml
